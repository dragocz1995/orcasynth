import { Activity } from 'lucide-react';
import type { ModuleMeta } from '../types';

export const meta: ModuleMeta = {
  id: 'timeline',
  label: 'Timeline',
  route: '/timeline',
  icon: Activity,
  group: 'Operate',
};
