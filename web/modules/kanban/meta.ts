import { KanbanSquare } from 'lucide-react';
import type { ModuleMeta } from '../types';

export const meta: ModuleMeta = {
  id: 'kanban',
  label: 'Kanban',
  route: '/kanban',
  icon: KanbanSquare,
  group: 'Operate',
};
